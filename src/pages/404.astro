---
import Base from '../layouts/Base.astro';
import { config } from '../config';
---

<Base title={`404 — ${config.name}`}>
  <div class="error-page">
    <h1>404</h1>
    <p>This page doesn't exist.</p>
    <a href="/">Go home</a>
  </div>
</Base>
